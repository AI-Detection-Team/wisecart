@* 📋 İSTER 2: Esnek View - Responsive tasarım, dinamik filtreleme, arama ve sayfalama *@
@* 📋 İSTER 7: ViewBag kullanımı - Controller'dan gelen veriler View'da kullanılıyor *@
@model IEnumerable<WiseCart_Web.Models.Product>

@{
    ViewData["Title"] = "Ürünler";
    int currentPage = ViewBag.CurrentPage;
    int totalPages = ViewBag.TotalPages;
    string currentCat = ViewBag.CurrentCategory;
    string currentSearch = ViewBag.CurrentSearch;
}

<div class="container mt-4">
    
    <div class="card shadow-sm mb-4 border-0 bg-light">
        <div class="card-body p-4">
            <form method="get" asp-action="Index" class="row g-3 align-items-end">
                
                <div class="col-md-4">
                    <label class="form-label fw-bold">Kategori</label>
                    <select name="category" class="form-select" onchange="this.form.submit()">
                        <option value="">Tüm Kategoriler</option>
                        @if (ViewBag.Categories != null)
                        {
                            foreach (var cat in ViewBag.Categories)
                            {
                                if (currentCat == cat) { <option value="@cat" selected>@cat</option> }
                                else { <option value="@cat">@cat</option> }
                            }
                        }
                    </select>
                </div>

                <div class="col-md-6">
                    <label class="form-label fw-bold">Arama</label>
                    <div class="input-group">
                        <input type="text" name="searchString" value="@currentSearch" class="form-control" placeholder="Marka veya Model ara...">
                        <button class="btn btn-primary" type="submit"><i class="fa-solid fa-search"></i> Ara</button>
                    </div>
                </div>

                <div class="col-md-2">
                    <a asp-action="Index" class="btn btn-outline-secondary w-100">Temizle</a>
                </div>
            </form>
        </div>
    </div>

    <div class="row row-cols-1 row-cols-md-3 row-cols-lg-4 g-4">
        @if (!Model.Any())
        {
            <div class="col-12 text-center py-5">
                <h3 class="text-muted">😔 Aradığınız kriterde ürün bulunamadı.</h3>
            </div>
        }
        
        @foreach (var item in Model) {
            <div class="col">
                <div class="card h-100 shadow-sm border-0 hover-card">
                    <div class="position-relative bg-white p-3 text-center" style="height: 200px;">
                        @if (User.Identity.IsAuthenticated)
                        {
                            <button class="favorite-btn position-absolute top-0 end-0 m-2 btn btn-sm border-0 rounded-circle shadow-sm" 
                                    data-product-id="@item.Id" 
                                    style="background: rgba(255, 255, 255, 0.9); z-index: 10; width: 40px; height: 40px; display: flex; align-items: center; justify-content: center;"
                                    title="Favorilere Ekle">
                                <i class="fa-regular fa-heart text-danger" style="font-size: 1.2rem;"></i>
                            </button>
                        }
                        <img src="@(string.IsNullOrEmpty(item.ImageUrl) ? "https://via.placeholder.com/300?text=Urun" : item.ImageUrl)" 
                             class="img-fluid" style="max-height: 100%; object-fit: contain;" 
                             alt="@item.Name"
                             onerror="this.src='https://via.placeholder.com/300?text=Resim+Yok'">
                    </div>
                    
                    <div class="card-body">
                        <div class="d-flex justify-content-between mb-2 small">
                            <span class="text-muted">@item.Category?.Name</span>
                        </div>
                        <h6 class="card-title text-truncate" title="@item.Name">@item.Name</h6>
                        <h5 class="text-success fw-bold mt-3">@item.CurrentPrice?.ToString("N0") TL</h5>
                    </div>
                    
                    <div class="card-footer bg-transparent border-0 pb-3">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary w-100 rounded-pill">
                            İncele & Analiz Et
                        </a>
                    </div>
                </div>
            </div>
        }
    </div>

    @if (totalPages > 1)
    {
        <nav class="mt-5 d-flex justify-content-center">
            <ul class="pagination">
                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                    <a class="page-link" asp-route-page="@(currentPage - 1)" asp-route-category="@currentCat" asp-route-searchString="@currentSearch">«</a>
                </li>
                
                <li class="page-item active"><span class="page-link">Sayfa @currentPage / @totalPages</span></li>

                <li class="page-item @(currentPage == totalPages ? "disabled" : "")">
                    <a class="page-link" asp-route-page="@(currentPage + 1)" asp-route-category="@currentCat" asp-route-searchString="@currentSearch">»</a>
                </li>
            </ul>
        </nav>
    }
</div>

<style>
    .hover-card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.15) !important; transition: 0.3s; }
    
    .favorite-btn {
        transition: all 0.3s ease;
    }
    
    .favorite-btn:hover {
        background: rgba(255, 255, 255, 1) !important;
        transform: scale(1.1);
    }
    
    .favorite-btn.active i {
        font-weight: 900;
    }
    
    .favorite-btn.active .fa-regular {
        display: none;
    }
    
    .favorite-btn.active .fa-solid {
        display: inline-block !important;
    }
    
    .favorite-btn .fa-solid {
        display: none;
    }
    
    /* Ürün kartındaki ilk kelime overlay'ini kaldır */
    .card .position-relative::before,
    .card .position-relative::after,
    .card-img-top::before,
    .card-img-top::after,
    .hover-card .position-relative::before,
    .hover-card .position-relative::after {
        display: none !important;
        content: none !important;
    }
    
    /* Ürün adının ilk kelimesini resmin üzerinden kaldır */
    .card-title::before,
    .card-title::after {
        display: none !important;
        content: none !important;
    }
    
    /* Resim container'ında overlay text'i kaldır */
    .card .position-relative > *:not(img):not(button) {
        display: none !important;
    }
</style>

@if (User.Identity.IsAuthenticated)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Tüm favori butonlarını kontrol et
            const favoriteButtons = document.querySelectorAll('.favorite-btn');
            
            favoriteButtons.forEach(btn => {
                const productId = parseInt(btn.getAttribute('data-product-id'));
                
                // Mevcut favori durumunu kontrol et
                fetch(`/api/Favorites/Check/${productId}`)
                    .then(response => response.json())
                    .then(data => {
                        if (data.isFavorite) {
                            btn.classList.add('active');
                            const icon = btn.querySelector('i');
                            icon.classList.remove('fa-regular');
                            icon.classList.add('fa-solid');
                        }
                    })
                    .catch(error => console.error('Favori kontrol hatası:', error));
                
                // Favori butonuna tıklama
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    const icon = btn.querySelector('i');
                    const isActive = btn.classList.contains('active');
                    
                    // Optimistic UI update
                    if (isActive) {
                        btn.classList.remove('active');
                        icon.classList.remove('fa-solid');
                        icon.classList.add('fa-regular');
                    } else {
                        btn.classList.add('active');
                        icon.classList.remove('fa-regular');
                        icon.classList.add('fa-solid');
                    }
                    
                    // API çağrısı
                    fetch('/api/Favorites/Toggle', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify(productId)
                    })
                    .then(response => response.json())
                    .then(data => {
                        if (data.message) {
                            // Toast bildirimi göster (eğer varsa)
                            if (typeof showToast === 'function') {
                                showToast(data.isFavorite ? 'success' : 'info', 
                                         data.isFavorite ? 'Favorilere Eklendi' : 'Favorilerden Çıkarıldı', 
                                         data.message);
                            }
                        }
                    })
                    .catch(error => {
                        console.error('Favori işlemi hatası:', error);
                        // Hata durumunda geri al
                        if (isActive) {
                            btn.classList.add('active');
                            icon.classList.remove('fa-regular');
                            icon.classList.add('fa-solid');
                        } else {
                            btn.classList.remove('active');
                            icon.classList.remove('fa-solid');
                            icon.classList.add('fa-regular');
                        }
                    });
                });
            });
        });
    </script>
}