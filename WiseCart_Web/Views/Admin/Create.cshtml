@model WiseCart_Web.Models.Product

@{
    ViewData["Title"] = "Yeni Ürün Ekle";
}

<div class="container mt-5" style="max-width: 700px;">
    <div class="card shadow-lg border-0">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0"><i class="fa-solid fa-box-open"></i> Yeni Ürün Ekle</h4>
        </div>
        <div class="card-body p-4">
            <form asp-action="Create" method="post">
                <div class="row g-3">
                    <div class="col-12">
                        <label class="form-label fw-bold">Ürün Adı</label>
                        <input asp-for="Name" class="form-control" placeholder="Örn: iPhone 13 128GB" required />
                    </div>
                    
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Fiyat (TL)</label>
                        <input asp-for="CurrentPrice" type="number" step="0.01" class="form-control" placeholder="25000" required />
                    </div>
                    
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Model Kodu</label>
                        <input asp-for="Model" class="form-control" placeholder="Örn: A2633" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Kategori</label>
                        <select asp-for="CategoryId" class="form-select" asp-items="@(new SelectList(ViewBag.Categories, "Id", "Name"))">
                            <option value="">-- Seçiniz --</option>
                        </select>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold">Marka</label>
                        <select asp-for="BrandId" class="form-select" asp-items="@(new SelectList(ViewBag.Brands, "Id", "Name"))">
                            <option value="">-- Seçiniz --</option>
                        </select>
                    </div>
                    
                    <div class="col-12">
                        <label class="form-label fw-bold">Resim URL</label>
                        <input asp-for="ImageUrl" class="form-control" placeholder="https://..." />
                    </div>

                    <div class="col-12 mt-4 d-flex justify-content-between">
                        <a asp-action="Index" class="btn btn-secondary">İptal</a>
                        <button type="submit" class="btn btn-success px-5">💾 Kaydet</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>