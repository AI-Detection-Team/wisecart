@model IEnumerable<WiseCart_Web.Models.Product>

@{
    ViewData["Title"] = "Ürünler";
}

<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold text-primary">🛍️ WiseCart Vitrini</h1>
        <p class="lead text-muted">En yeni teknoloji ürünlerini keşfedin ve Yapay Zeka ile fiyat analizi yapın.</p>
    </div>

    <div class="row row-cols-1 row-cols-md-3 g-4">
        @foreach (var item in Model) {
            <div class="col">
                <div class="card h-100 shadow-sm border-0 hover-effect">
                    <div class="bg-light d-flex align-items-center justify-content-center" style="height: 250px;">
                        <img src="https://via.placeholder.com/300x200?text=@(item.Category?.Name ?? "Urun")" 
                             class="card-img-top p-3" alt="@item.Name" 
                             style="max-height: 100%; object-fit: contain;">
                    </div>
                    
                    <div class="card-body">
                        <div class="d-flex justify-content-between mb-2">
                            <span class="badge bg-info text-dark">@item.Brand?.Name</span>
                            <span class="badge bg-secondary">@item.Category?.Name</span>
                        </div>
                        <h5 class="card-title text-truncate" title="@item.Name">@item.Name</h5>
                        <h4 class="text-success fw-bold mt-3">@item.CurrentPrice?.ToString("N0") TL</h4>
                    </div>
                    
                    <div class="card-footer bg-white border-top-0 pb-3">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-primary w-100">
                            🔍 İncele & AI Analiz
                        </a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    .hover-effect:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
        transition: all 0.3s ease;
    }
</style>